# ARCHITECTURE.md — [프로젝트명] 기술 아키텍처

> 기술 스택, 디렉토리 구조, 핵심 패턴 정의.
> 구현 결정의 근거가 되는 문서. CONSTITUTION > ARCHITECTURE.md 순위.

---

## 1. 기술 스택

| 레이어 | 기술 | 버전 | 선택 이유 |
|--------|------|------|----------|
| 언어 | | | |
| Frontend | | | |
| Backend / DB | | | |
| 인증 | | | |
| AI / 외부 API | | | |
| 배포 | | | |
| 테스트 | | | |

---

## 2. 디렉토리 구조

```
project-root/
├── (프로젝트 구조를 여기에 기술)
├── docs/
│   ├── exec/
│   │   ├── open-decisions.md
│   │   ├── tech-debt.md
│   │   ├── NEXT_SESSION.md
│   │   └── lessons.md
│   └── product-specs/
└── .claude/          ← agent_runtime 복사본
```

---

## 3. 핵심 아키텍처 패턴

### 3-1. 데이터 흐름

(데이터가 어떻게 흐르는지 — UI → 상태 → API → DB 등)

### 3-2. 상태 관리

(상태 관리 방식 및 원칙)

### 3-3. 에러 처리 패턴

(프로젝트 전반에 적용되는 에러 처리 방식)

---

## 4. 핵심 제약 (아키텍처 원칙)

에이전트가 구현 시 반드시 따라야 할 구조적 제약.

1. (예: 모든 DB 접근은 repository 레이어를 통해서만)
2. (예: 비즈니스 로직은 UI 레이어에 직접 두지 않는다)
3. (예: 외부 API 호출은 service 레이어에서만)

---

## 5. 환경 구성

### 환경 변수 (필수)
```
# .env 예시
API_KEY=
DATABASE_URL=
```

### 개발 환경 실행
```bash
# 설치
[설치 명령]

# 개발 서버
[실행 명령]

# 검증 (typecheck / lint / test)
[검증 명령]
```

---

## 6. 주요 의존성

| 패키지 | 버전 | 용도 |
|--------|------|------|
| | | |

---

## 개정 이력

| 버전 | 날짜 | 변경 내용 |
|------|------|----------|
| v1.0 | YYYY-MM-DD | 초안 |
